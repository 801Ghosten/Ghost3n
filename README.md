Pentesting relatied information, traning, code snips based in html docs for loaclly hosteded webserver like nginx. 
Use sudo apt install nginx
rename file to html 
sudo cp html /var/www/
go to localhost in browser
